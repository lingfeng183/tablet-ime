# Tablet IME

Android平板中文输入法，支持中英文输入和功能键。

## 功能特性

- **中英文输入**: 支持拼音输入和英文输入
- **功能键支持**: 
  - F1-F12 功能键
  - 方向键 (↑↓←→)
  - 系统功能键 (Esc, Tab, Enter, Shift, Ctrl, Alt, Caps)
  - Home, End, Insert 键
  - 数字键 1-9, 0
- **拼音输入**: 支持拼音输入中文
- **候选词选择**: 显示中文候选词列表
- **中英文切换**: 点击 🇨🇳/🇺🇸 按钮切换

## 安装步骤

1. 安装Flutter SDK (https://flutter.dev/docs/get-started/install)
2. 克隆项目到本地
3. 在项目目录运行:
   ```bash
   flutter pub get
   flutter build apk
   ```
4. 在Android设备上安装APK

## 启用输入法

安装后:
1. 打开系统设置 > 语言和输入法
2. 找到 "Tablet IME" 并启用
3. 选择 "Tablet IME" 作为当前输入法

## 项目结构

```
tablet_ime/
├── lib/
│   ├── main.dart              # 主入口
│   ├── keyboard_layout.dart   # 键盘布局
│   ├── keyboard_state.dart    # 键盘状态管理
│   ├── keyboard_service.dart  # 输入服务
│   └── key_button.dart        # 按键组件
├── android/
│   └── app/src/main/
│       ├── AndroidManifest.xml
│       ├── java/.../TabletInputMethodService.java  # 输入法服务
│       └── res/xml/method.xml  # 输入法配置
└── pubspec.yaml
```

## 使用说明

### 中文输入
1. 确保显示🇨🇳图标(中文模式)
2. 输入拼音字母
3. 从候选词列表选择汉字
4. 按空格键选择第一个候选词

### 英文输入
1. 点击🇨🇳/🇺🇸切换到🇺🇸图标
2. 直接输入字母

### 功能键使用
- F1-F12: 直接点击对应功能键
- 方向键: 点击↑↓←→导航光标
- Ctrl/Shift/Alt: 按住组合其他键使用
- Back: 删除字符
- Enter: 确认输入或换行

## 依赖

- Flutter SDK >= 3.0.0
- Android SDK >= 24
- Dart >= 3.0.0
- provider: ^6.1.1

## 许可证

MIT License
